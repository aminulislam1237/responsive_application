import 'package:flutter/material.dart';
class moiblilebody extends StatelessWidget {
  const moiblilebody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
      appBar: AppBar(
        title: Text("Phone page"),
      )
    );
  }
}
